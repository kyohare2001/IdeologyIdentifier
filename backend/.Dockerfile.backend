# 基盤となるPythonイメージを指定
FROM python:3.9-slim

# 作業ディレクトリを設定
WORKDIR /app

# 必要なパッケージをインストール
# ※ os, time は標準ライブラリのためインストール不要
RUN pip install --no-cache-dir \
    openai \
    pandas \
    matplotlib \
    python-dotenv \
    atproto \
    flask 

# 現在のディレクトリ内のファイルをコンテナ内にコピー
COPY . .

# コンテナ起動時に実行するコマンド（例として main.py を実行）
CMD ["python", "main.py"]